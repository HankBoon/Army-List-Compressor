// To Do:
// Anzahl der Ausgaben von datasheet stimmt nicht   ->erledigt!
// Liste taucht ausserhalb von p auf -> erledigt!
// entscheiden, ob ein paragraph oder mehrere am Anfang besser sind -> erledigt!
// ist es möglich Aufzählungszeichen statt einer Liste zu nutzen? -> erledigt!
// button einfügen für "copy to clipboard" -> erledigt!
// reset am anfang einbauen damit die die ul nicht ständig erweitert wird sondern erneuert-> erledigt!
// Ausrüstungsoptionen einfügen -> erledigt!
// Machen Schalter für verschiedene Armeen Sinn?
// Schalter für verschiedene Armeelisten tools? bzw. automatische erkennung der verwendeten tools
// Optionsschalter für Anzeige der Ausrüstung?
// css aufräumen!-> erledigt!
// backlog  Trello
// regex für
// Daten auslagern -> erledigt!
// Markdown umsetzen ->erledigt!
// Alert mit anderer Benachrichtigungs- umsetzen
// reset button einfügen -> erledigt!
// Punktwerte einfügen -> erledigt
// enhancemnts einfügen -> erledigt!
// warlord einfügen -> erledigt!
// data aufräumen -> erledigt!
// breite der textarea fixieren
// outputArea an größe des inhalts anpassen
// outputArea.textContent anpassen. inhalt und logik trennen.inhalt in variable zusammenfassen und erst am ende das ergebnis ausgeben. -> erledigt!
// output area - textcontent ändern. so dass es erst ganz am ende mit inhalt befüllt wir und nicht in mehreren schritten
//anlegen: object oder array dass sich aus den zeilen und gefundenen ausrüstungen speisst -> erledigt!
// struktur so ändern, dass nicht nur tauEmpire durchsucht wird, sondern jede x beliebige Armee.
// Ausrüstung die mehrmals vorhanden ist, einfügen! -> erledigt!
// Rechtschreibung in object überprüfen
// Funktion einfügen, so dass Enter-Taste auch die HAuptfunktion startet
// info auf Seite einfügen, die erklärt , was es ist und welche Funktionen behinhaltet sind.
// contakt einfügen
// bugfixes anzeigen
// if loop ketten verschlanken

//weapon.count aufaddieren, dammit nicht mehrmals der selbe Eintrag auftaucht. 

// Bugs:
// etheral gefunden, obwohl nicht in Liste -> tritt auf weil etheral in titel vorkommt. -> titel von searcharea ausschließen! )>erledigt!
// enhancements werden nicht zu punkten gezählt ->erledigt!
// anzahl von pirahanas und Broadsides ist immer "1". Punkte werden nicht aufadiert. ->erledigt! 
// broasides waffen anzeige checken  ->erledigt!
//enhancements nicht auf chars geadded ->erledigt!

// zu langer Titel verursacht scheinbar Probleme. -> definition ändern! nicht nur firstLine , sondern firstLine bis \n\n ->erledigt!

// funktionen ändern, so dass sie nur mit regex teile der jeweiligen zeile in den output kopieren und nicht aus dem tau.objekt 
// setAllUnits grundlegend ändern. cases einführen. Wenn true, dann push to output array. ganz am ende outputfield mit array füllen.

// krootox rampagers wird 2x mal gefunden durch die index funktionen ->erledigt!
// 20er kroot trupp wird als 30er angezeigt-> erledigt!

// umstrukturieren so dass am anfang ein array mit allen gefundenen keywords angelegt wird. aus diesen dann erst die searcharea definieren.
// aus irgendeinem grund erkennt er bei holodiscus nicht die leeren zeilen. bzw es es ind keine vorhanden ->erledigt!